export default function Section({
	               selectedDate,
	               sectionData,
	               setSectionData,
	               setSection,
	               timeData,
	               setSelectedTime,
	               checked,
	               setChecked,
	              }){
	return(

		       <div>
  
    </div>
		)
}